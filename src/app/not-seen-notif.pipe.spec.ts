import { NotSeenNotifPipe } from './not-seen-notif.pipe';

describe('NotSeenNotifPipe', () => {
  it('create an instance', () => {
    const pipe = new NotSeenNotifPipe();
    expect(pipe).toBeTruthy();
  });
});
